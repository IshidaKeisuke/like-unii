table:
  name: users
  schema: public
array_relationships:
  - name: matches
    using:
      foreign_key_constraint_on:
        column: users_id
        table:
          name: matches
          schema: public
